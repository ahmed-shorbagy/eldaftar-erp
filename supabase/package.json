{
  "name": "eldafttar-connections",
  "private": true,
  "type": "module",
  "scripts": {
    "verify:supabase": "node verify-connections.mjs --supabase-only",
    "verify:r2": "node verify-connections.mjs --r2-only",
    "verify:all": "node verify-connections.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1138.0",
    "dotenv": "^18.0.3"
  }
}
